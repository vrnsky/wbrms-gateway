spring:
  application:
    name: wbrms-gateway
  cloud:
    gateway:
      routes:
        - id: billingService
          uri: http://sin.wbrms-menu-service.internal
          predicates:
            - Path=/menu/**